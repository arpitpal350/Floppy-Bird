name: floppy_bird
description: A polished, original Flutter + Flame arcade game.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ^3.11.0
dependencies:
  flutter:
    sdk: flutter
  flame: ^1.38.0
  shared_preferences: ^2.5.4
  unity_ads_plugin: ^0.4.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
